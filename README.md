# SimpleCalculator
Responsive web calculator built from scratch with semantic HTML, CSS Grid, and JS (OOP). Features keyboard event listeners, memory cleanup handlers, floating-point precision error handling, and active interaction scaling—all styled inside a premium dark-themed interface.
